# Лекция: Циклы в Python. `while`, `for`, `break`, `continue`

**Дисциплина:** Основы алгоритмизации и программирования (Python)  
**Раздел 2.** Управляющие конструкции  
**Тема:** Циклы с предусловием (`while`) и с параметром (`for`). Операторы `break`, `continue`  
**Тип занятия:** лекция (2 ч)

---

## 1. Цели и задачи

**Цель:** научиться использовать циклы для повторяющихся действий.

**Задачи:**
- Освоить цикл `while` — с предусловием.
- Освоить цикл `for` — с параметром.
- Изучить операторы `break` и `continue`.
- Разобраться, когда какой цикл использовать.
- Научиться работать с `range()`.

**Формируемые компетенции:**
- **ОК 01** — выбирать способы решения задач.
- **ОК 02** — использовать средства поиска и анализа информации.
- **ПК 1.1** — формировать алгоритмы.

---

## 2. Что такое цикл

**Цикл** — это конструкция, которая **повторяет** блок команд несколько раз.

**Зачем нужны циклы:**
- Обработать много элементов.
- Повторять действие до условия.
- Не дублировать код.

**Типы циклов в Python:**

| Цикл | Когда использовать |
|------|-------------------|
| **`while`** | Когда число повторений **неизвестно** заранее |
| **`for`** | Когда число повторений **известно** или есть коллекция |

---

## 3. Цикл `while` — с предусловием

### 3.1. Синтаксис

```python
while условие:
    тело цикла
```

**Как работает:**
1. Проверяется условие.
2. Если `True` — выполняется тело.
3. Снова проверяется условие.
4. Если `False` — цикл завершается.

⚠️ **Условие проверяется ПЕРЕД каждым выполнением.** Отсюда название «с предусловием».

### 3.2. Простой пример

```python
i = 1
while i <= 5:
    print(i)
    i += 1
```

**Вывод:**
```
1
2
3
4
5
```

**Пошагово:**

| Итерация | `i` | Условие `i <= 5` | Что делаем |
|:--------:|:---:|:----------------:|------------|
| 1 | 1 | True | print(1), i = 2 |
| 2 | 2 | True | print(2), i = 3 |
| 3 | 3 | True | print(3), i = 4 |
| 4 | 4 | True | print(4), i = 5 |
| 5 | 5 | True | print(5), i = 6 |
| 6 | 6 | False | Выход |

### 3.3. Обязательно менять условие

⚠️ **Если не менять переменную — цикл будет бесконечным:**

```python
i = 1
while i <= 5:
    print(i)         # ❌ i не меняется — бесконечный цикл!
```

**Что нужно:**

```python
i = 1
while i <= 5:
    print(i)
    i += 1           # ✅ i увеличивается
```

### 3.4. Бесконечный цикл с выходом

```python
while True:
    command = input("Введите команду (quit для выхода): ")
    if command == "quit":
        break
    print(f"Вы ввели: {command}")
```

**Идея:** цикл работает, пока пользователь не введёт `quit`.

### 3.5. Когда использовать `while`

- Число повторений **неизвестно**.
- Ожидание события (ввод, сигнал).
- Чтение до конца файла.
- Игра «угадай число».

---

## 4. Цикл `for` — с параметром

### 4.1. Синтаксис

```python
for переменная in последовательность:
    тело цикла
```

**Как работает:**
1. Берётся **первый** элемент последовательности.
2. Выполняется тело.
3. Берётся **следующий** элемент.
4. И так до конца последовательности.

### 4.2. Функция `range()`

**`range()`** создаёт последовательность чисел.

| Запись | Что создаёт | Пример |
|--------|-------------|--------|
| `range(5)` | 0, 1, 2, 3, 4 | 5 чисел |
| `range(2, 6)` | 2, 3, 4, 5 | от 2 до 6 |
| `range(0, 10, 2)` | 0, 2, 4, 6, 8 | с шагом 2 |
| `range(10, 0, -1)` | 10, 9, 8, ..., 1 | обратный |

⚠️ **Правая граница НЕ включается.** `range(1, 5)` → `1, 2, 3, 4`.

### 4.3. Простые примеры

```python
# Вывод 0..4
for i in range(5):
    print(i)

# Вывод 1..5
for i in range(1, 6):
    print(i)

# Чётные числа от 0 до 10
for i in range(0, 11, 2):
    print(i)

# Обратный счёт
for i in range(5, 0, -1):
    print(i)
```

### 4.4. Перебор строки

```python
word = "Python"
for letter in word:
    print(letter)
```

**Вывод:**
```
P
y
t
h
o
n
```

### 4.5. Перебор списка

```python
fruits = ["яблоко", "банан", "вишня"]
for fruit in fruits:
    print(fruit)
```

### 4.6. Когда использовать `for`

- Число повторений **известно**.
- Перебор элементов коллекции.
- Обход диапазона чисел.
- Обработка строк, списков, словарей.

---

## 5. `while` vs `for`

| Критерий | `while` | `for` |
|----------|:-------:|:-----:|
| Число повторений | Неизвестно | Известно |
| Условие | Любое | Перебор |
| Переменная | Меняем сами | Автоматически |
| Опасность бесконечности | Есть | Нет |
| Типичное применение | Ввод, ожидание | Диапазон, коллекция |

**Правило:**
- **Знаешь, сколько раз** → `for`.
- **Не знаешь** → `while`.

---

## 6. Оператор `break` — прервать цикл

**`break`** — **немедленно** выходит из цикла.

### 6.1. Пример с `for`

```python
for i in range(1, 11):
    if i == 5:
        break
    print(i)
```

**Вывод:**
```
1
2
3
4
```

**Что произошло:** на `i = 5` цикл прервался.

### 6.2. Пример с `while`

```python
i = 1
while i <= 10:
    if i == 5:
        break
    print(i)
    i += 1
```

**Вывод:** тот же.

### 6.3. Поиск элемента

```python
numbers = [3, 7, 1, 9, 5]
target = 9

for i, num in enumerate(numbers):
    if num == target:
        print(f"Найдено на позиции {i}")
        break
else:
    print("Не найдено")
```

⚠️ **`else` после `for`** — выполняется, если цикл **не был прерван** через `break`.

---

## 7. Оператор `continue` — пропустить итерацию

**`continue`** — **пропускает** текущую итерацию и переходит к следующей.

### 7.1. Пример с `for`

```python
for i in range(1, 6):
    if i == 3:
        continue
    print(i)
```

**Вывод:**
```
1
2
4
5
```

**Что произошло:** на `i = 3` итерация пропущена.

### 7.2. Пропуск чётных

```python
for i in range(1, 11):
    if i % 2 == 0:
        continue
    print(i)
```

**Вывод:** все нечётные от 1 до 10.

### 7.3. Пропуск пустых строк

```python
lines = ["Привет", "", "Мир", "", "!"]
for line in lines:
    if not line:
        continue
    print(line)
```

---

## 8. `break` vs `continue`

| Оператор | Что делает |
|:--------:|-----------|
| **`break`** | Выходит из цикла **полностью** |
| **`continue`** | Пропускает **одну** итерацию, продолжает цикл |

**Схема:**

```
break:      итерация 1 → итерация 2 → break → ВЫХОД
continue:   итерация 1 → continue → итерация 3 → ...
```

---

## 9. Вложенные циклы

Цикл **внутри** цикла.

```python
for i in range(1, 4):
    for j in range(1, 4):
        print(f"{i} × {j} = {i * j}")
    print("---")
```

**Вывод:**
```
1 × 1 = 1
1 × 2 = 2
1 × 3 = 3
---
2 × 1 = 2
2 × 2 = 4
2 × 3 = 6
---
3 × 1 = 3
3 × 2 = 6
3 × 3 = 9
---
```

### Таблица умножения

```python
for i in range(1, 10):
    for j in range(1, 10):
        print(f"{i*j:3}", end=" ")
    print()
```

⚠️ **`break` во вложенном цикле** выходит только из **внутреннего**.

---

## 10. Полезные приёмы

### 10.1. `enumerate()` — индекс + значение

```python
fruits = ["яблоко", "банан", "вишня"]
for i, fruit in enumerate(fruits):
    print(f"{i}: {fruit}")
```

### 10.2. `zip()` — параллельный перебор

```python
names = ["Иван", "Пётр"]
ages = [20, 25]

for name, age in zip(names, ages):
    print(f"{name} — {age}")
```

### 10.3. `else` после цикла

```python
for i in range(5):
    if i == 10:
        break
else:
    print("Не нашли 10")
```

**`else`** выполняется, если цикл **завершился без `break`**.

---

## 11. Типичные ошибки

| Ошибка | Что не так | Решение |
|--------|-----------|---------|
| `while i <= 5:` без `i += 1` | Бесконечный цикл | Изменять переменную |
| `for i in 5:` | `range` забыли | `for i in range(5):` |
| `range(1, 5)` → ожидали 1..5 | Правая граница не включается | `range(1, 6)` |
| Забыли `:` | Синтаксис | `while условие:` |
| Забыли отступ | Синтаксис | 4 пробела |
| `break` во вложенном цикле | Выходит только из внутреннего | Использовать флаг |
| Изменение коллекции в цикле | Непредсказуемо | Итерировать по копии |

**Пример ошибки:**

```python
for i in range(5):
    print(i)
    i += 1          # ❌ бессмысленно: i всё равно берётся из range
```

---

## 12. Практические примеры

### Пример 1. Сумма чисел 1..100

```python
total = 0
for i in range(1, 101):
    total += i
print(total)   # 5050
```

### Пример 2. Факториал

```python
n = 5
fact = 1
for i in range(1, n + 1):
    fact *= i
print(fact)   # 120
```

### Пример 3. Угадай число

```python
import random

secret = random.randint(1, 100)
while True:
    guess = int(input("Угадайте число: "))
    if guess == secret:
        print("Правильно!")
        break
    elif guess < secret:
        print("Больше")
    else:
        print("Меньше")
```

### Пример 4. Таблица умножения

```python
for i in range(1, 10):
    for j in range(1, 10):
        print(f"{i*j:4}", end="")
    print()
```

### Пример 5. Сумма цифр числа

```python
n = 12345
total = 0
while n > 0:
    total += n % 10
    n //= 10
print(total)   # 15
```

---

## 13. Итоги

- **`while`** — цикл с предусловием.
- **`for`** — цикл по последовательности.
- **`range()`** — генератор чисел.
- **`break`** — выйти из цикла.
- **`continue`** — пропустить итерацию.
- **Вложенные циклы** — цикл в цикле.
- **`enumerate`, `zip`** — удобные приёмы.
- **`else` после цикла** — если не было `break`.

---

## 14. Вопросы для самоконтроля

1. Чем `while` отличается от `for`?
2. Что такое `range()`? Какие у него параметры?
3. Что делает `break`?
4. Что делает `continue`?
5. Что произойдёт, если забыть изменить переменную в `while`?
6. Включается ли правая граница в `range(1, 5)`?
7. Как выйти из вложенного цикла?
8. Что делает `else` после цикла?

---

## 15. Домашнее задание

1. Вывести чётные числа от 1 до 20.
2. Найти сумму чисел от 1 до 100.
3. Найти факториал числа `n`.
4. Игра «угадай число» с подсказками.
5. Вывести таблицу умножения 5×5.
6. Найти сумму цифр числа.
7. Ответить на вопросы самоконтроля.

---

## 16. Литература

1. Лутц М. «Программирование на Python». — Т. 1.
2. Мэтиз Э. «Изучаем Python». — Гл. 7.
3. Официальная документация Python: docs.python.org.

---

