# Лекция: Кортежи в Python. Отличия от списков. Когда использовать

**Дисциплина:** Основы алгоритмизации и программирования (Python)  
**Раздел 3.** Структуры данных  
**Тема:** Кортежи. Отличия от списков. Когда использовать  
**Тип занятия:** лекция (2 ч)

---

## 1. Цели и задачи

**Цель:** изучить кортежи, понять их отличия от списков и научиться выбирать подходящую структуру данных.

**Задачи:**
- Освоить создание кортежей.
- Изучить операции над кортежами.
- Разобраться с распаковкой.
- Понять отличия кортежей от списков.
- Научиться выбирать между кортежем и списком.

**Формируемые компетенции:**
- **ОК 01** — выбирать способы решения задач.
- **ОК 02** — использовать средства поиска и анализа информации.
- **ПК 1.1** — формировать алгоритмы.

---

## 2. Что такое кортеж

**Кортеж (`tuple`)** — это **упорядоченная неизменяемая** коллекция элементов.

**Ключевые свойства:**
- **Упорядоченный** — элементы имеют индексы.
- **Неизменяемый** — после создания нельзя изменить.
- **Может содержать разные типы** — числа, строки, списки.
- **Может содержать дубликаты**.
- **Хэшируемый** — можно использовать как ключ словаря (если элементы хэшируемы).

**Создание кортежа:**

```python
# Через круглые скобки
point = (3, 5)

# Без скобок (тоже кортеж)
point = 3, 5

# Пустой кортеж
empty = ()

# Кортеж из одного элемента — ЗАПЯТАЯ обязательна!
one = (42,)      # ✅ кортеж
not_tuple = (42) # ❌ это просто число

# Через tuple()
nums = tuple([1, 2, 3])   # (1, 2, 3)
```

⚠️ **Кортеж из одного элемента** требует **запятой**: `(42,)`, а не `(42)`.

---

## 3. Операции над кортежами

### 3.1. Доступ по индексу

```python
point = (3, 5)
print(point[0])    # 3
print(point[1])    # 5

# Отрицательные индексы
print(point[-1])   # 5
print(point[-2])   # 3
```

### 3.2. Срезы

```python
nums = (10, 20, 30, 40, 50)

print(nums[1:3])    # (20, 30)
print(nums[:3])     # (10, 20, 30)
print(nums[2:])     # (30, 40, 50)
print(nums[::-1])   # (50, 40, 30, 20, 10)
```

### 3.3. Конкатенация и повторение

```python
a = (1, 2)
b = (3, 4)

print(a + b)        # (1, 2, 3, 4)
print(a * 3)        # (1, 2, 1, 2, 1, 2)
```

### 3.4. Проверка вхождения

```python
point = (3, 5)
print(3 in point)      # True
print(10 in point)     # False
print(10 not in point) # True
```

### 3.5. Длина, минимум, максимум, сумма

```python
nums = (5, 2, 8, 1)

print(len(nums))    # 4
print(min(nums))    # 1
print(max(nums))    # 8
print(sum(nums))    # 16
```

### 3.6. Методы кортежей

Их **всего два**:

| Метод | Что делает |
|-------|-----------|
| `count(x)` | Сколько раз встречается `x` |
| `index(x)` | Позиция первого `x` |

```python
nums = (1, 2, 3, 2, 4, 2)

print(nums.count(2))   # 3
print(nums.index(3))   # 2
```

---

## 4. Распаковка кортежей

### 4.1. Простая распаковка

```python
point = (3, 5)
x, y = point
print(x, y)   # 3 5
```

### 4.2. Распаковка с `*`

```python
nums = (1, 2, 3, 4, 5)
first, *middle, last = nums

print(first)    # 1
print(middle)   # [2, 3, 4]
print(last)     # 5
```

⚠️ **`*middle` собирает элементы в список**, а не в кортеж.

### 4.3. Обмен значений

```python
a, b = 5, 10
a, b = b, a       # обмен через кортеж
print(a, b)       # 10 5
```

### 4.4. Возврат нескольких значений из функции

```python
def min_max(nums):
    return min(nums), max(nums)   # возвращает кортеж

lo, hi = min_max([3, 1, 7, 2])
print(lo, hi)   # 1 7
```

⚠️ **Это одна из главных причин использовать кортежи.**

### 4.5. Итерация

```python
points = [(1, 2), (3, 4), (5, 6)]

for x, y in points:
    print(f"x={x}, y={y}")
```

---

## 5. Кортежи vs списки

### 5.1. Основные отличия

| Критерий | Список `list` | Кортеж `tuple` |
|----------|:-------------:|:--------------:|
| Синтаксис | `[1, 2, 3]` | `(1, 2, 3)` |
| Изменяемость | ✅ Изменяемый | ❌ Неизменяемый |
| Методы | Много | Два (`count`, `index`) |
| Скорость | Медленнее | **Быстрее** |
| Память | Больше | **Меньше** |
| Хэшируемость | ❌ | ✅ (если элементы хэшируемы) |
| Использование как ключ словаря | ❌ | ✅ |
| Защита от изменений | ❌ | ✅ |

### 5.2. Что можно, а что нельзя

| Операция | Список | Кортеж |
|----------|:------:|:------:|
| Доступ по индексу | ✅ | ✅ |
| Срез | ✅ | ✅ |
| `len()`, `min()`, `max()`, `sum()` | ✅ | ✅ |
| `in` | ✅ | ✅ |
| `+`, `*` | ✅ | ✅ |
| Изменение элемента | ✅ | ❌ |
| `append()` | ✅ | ❌ |
| `remove()` | ✅ | ❌ |
| `sort()` | ✅ | ❌ |
| Использовать как ключ словаря | ❌ | ✅ |

### 5.3. Пример: попытка изменить

```python
# Список — можно
lst = [1, 2, 3]
lst[0] = 100      # ✅
print(lst)        # [100, 2, 3]

# Кортеж — нельзя
tup = (1, 2, 3)
tup[0] = 100      # ❌ TypeError
```

**Ошибка:**
```
TypeError: 'tuple' object does not support item assignment
```

### 5.4. Преобразование

```python
# Список → кортеж
lst = [1, 2, 3]
tup = tuple(lst)      # (1, 2, 3)

# Кортеж → список
tup = (1, 2, 3)
lst = list(tup)       # [1, 2, 3]
```

### 5.5. Хэшируемость

```python
# Кортеж можно использовать как ключ словаря
data = {
    (1, 2): "точка A",
    (3, 4): "точка B"
}
print(data[(1, 2)])   # точка A

# Список — нельзя
data = {
    [1, 2]: "точка"    # ❌ TypeError
}
```

---

## 6. Когда использовать кортеж, а когда список

### 6.1. Кортеж — когда:

✅ **Данные не должны меняться** — координаты точки, цвета RGB.

```python
RED = (255, 0, 0)
point = (10, 20)
```

✅ **Нужна защита от изменений** — константы.

```python
DAYS = ("Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс")
```

✅ **Возврат нескольких значений** из функции.

```python
def get_user():
    return "Иван", 20
```

✅ **Использование как ключ словаря.**

```python
cache = {}
cache[(1, 2)] = "значение"
```

✅ **Распаковка.**

```python
x, y, z = (1, 2, 3)
```

✅ **Экономия памяти и скорость.**

### 6.2. Список — когда:

✅ **Данные должны меняться** — корзина покупок, список задач.

```python
cart = ["яблоко", "банан"]
cart.append("вишня")
```

✅ **Нужны методы** — `append`, `remove`, `sort`.

```python
tasks = ["задача 1", "задача 2"]
tasks.remove("задача 1")
tasks.sort()
```

✅ **Размер коллекции меняется.**

```python
results = []
for i in range(10):
    results.append(i * i)
```

✅ **Нужна сортировка.**

```python
numbers = [3, 1, 4, 1, 5]
numbers.sort()
```

---

## 7. Практические примеры

### 7.1. Координаты точки

```python
point = (3, 5)
x, y = point
print(f"Точка на ({x}, {y})")
```

### 7.2. Цвета RGB

```python
RED = (255, 0, 0)
GREEN = (0, 255, 0)
BLUE = (0, 0, 255)

def print_color(color):
    r, g, b = color
    print(f"RGB({r}, {g}, {b})")

print_color(RED)
```

### 7.3. Возврат нескольких значений

```python
def divide(a, b):
    quotient = a // b
    remainder = a % b
    return quotient, remainder   # кортеж

q, r = divide(17, 5)
print(f"17 = 5 × {q} + {r}")   # 17 = 5 × 3 + 2
```

### 7.4. Список кортежей

```python
students = [
    ("Иван", 20),
    ("Пётр", 22),
    ("Анна", 19)
]

for name, age in students:
    print(f"{name}: {age}")
```

### 7.5. Словарь с кортежами-ключами

```python
distances = {
    ("Москва", "Питер"): 700,
    ("Москва", "Казань"): 800,
}

print(distances[("Москва", "Питер")])   # 700
```

### 7.6. Константы

```python
PI = 3.14159
COLORS = ("красный", "зелёный", "синий")
WEEKDAYS = ("Пн", "Вт", "Ср", "Чт", "Пт")
```

---

## 8. Типичные ошибки

| Ошибка | Что не так | Решение |
|--------|-----------|---------|
| `tup[0] = 5` | Кортеж неизменяем | Использовать список |
| `(42)` вместо `(42,)` | Это число, не кортеж | Добавить запятую |
| `tup.append(5)` | У кортежа нет `append` | Использовать список |
| `[1, 2]` как ключ словаря | Список не хэшируется | Использовать кортеж |
| Изменение вложенного списка | Внутренний список можно менять | Осторожно с вложенностью |
| `tup.sort()` | Нет метода `sort` | `sorted(tup)` |

**Пример ловушки:**

```python
tup = (1, [2, 3], 4)
tup[1].append(5)   # ✅ можно — изменяем список внутри
print(tup)         # (1, [2, 3, 5], 4)

tup[1] = [10]      # ❌ нельзя — заменяем элемент кортежа
```

⚠️ **Кортеж неизменяем, но вложенные объекты — можно менять.**

---

## 9. Сравнение производительности

```python
import timeit

# Создание
t_list = timeit.timeit("[1, 2, 3]", number=1000000)
t_tuple = timeit.timeit("(1, 2, 3)", number=1000000)

print(f"Список: {t_list:.4f}")
print(f"Кортеж: {t_tuple:.4f}")
```

**Результат (примерно):**
- Список: `0.05`
- Кортеж: `0.02`

**Кортеж быстрее в 2–3 раза.**

**Память:**

```python
import sys

lst = [1, 2, 3]
tup = (1, 2, 3)

print(sys.getsizeof(lst))   # 88 байт
print(sys.getsizeof(tup))   # 64 байта
```

⚠️ **Кортеж занимает меньше памяти.**

---

## 10. Итоги

- **Кортеж** — упорядоченная **неизменяемая** коллекция.
- Создаётся через `()` или `tuple()`.
- **Один элемент** — `(42,)` с запятой.
- **Два метода:** `count`, `index`.
- **Распаковка** — сильная сторона кортежей.
- **Отличия от списка:** неизменяемость, скорость, память, хэшируемость.
- **Кортеж — когда данные не меняются.**
- **Список — когда данные меняются.**

---

## 11. Вопросы для самоконтроля

1. Что такое кортеж?
2. Чем кортеж отличается от списка?
3. Как создать кортеж из одного элемента?
4. Какие методы есть у кортежа?
5. Что такое распаковка?
6. Почему кортеж быстрее списка?
7. Когда использовать кортеж?
8. Почему кортеж можно использовать как ключ словаря?
9. Можно ли изменить элемент кортежа?
10. Как преобразовать список в кортеж?

---

## 12. Домашнее задание

1. Создать кортеж из 5 чисел, вывести его.
2. Распаковать кортеж в переменные.
3. Написать функцию, возвращающую min и max через кортеж.
4. Создать список кортежей — координаты точек.
5. Использовать кортеж как ключ словаря.
6. Сравнить скорость списка и кортежа.
7. Ответить на вопросы самоконтроля.

---

## 13. Литература

1. Лутц М. «Программирование на Python». — Т. 1.
2. Мэтиз Э. «Изучаем Python». — Гл. 4.
3. Официальная документация Python: docs.python.org.

---
