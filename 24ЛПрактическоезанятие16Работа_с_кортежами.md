# Практическое занятие 16. Работа с кортежами: распаковка, возврат нескольких значений

**Дисциплина:** Основы алгоритмизации и программирования (Python)  
**Раздел 3.** Структуры данных  
**Тема:** Работа с кортежами: распаковка, возврат нескольких значений  
**Тип занятия:** практическое (2 ч)

---

## 1. Цели и задачи

**Цель:** закрепить навыки работы с кортежами, распаковкой и возвратом нескольких значений из функций.

**Задачи:**
- Освоить создание и доступ к кортежам.
- Научиться распаковывать кортежи.
- Изучить возврат нескольких значений через `return`.
- Применить распаковку в задачах.
- Разобраться с `*` (звёздочкой) при распаковке.

**Формируемые компетенции:**
- **ОК 01** — выбирать способы решения задач.
- **ОК 02** — использовать средства поиска и анализа информации.
- **ПК 1.1** — формировать алгоритмы.

---

## 2. Краткая теория

### 2.1. Создание кортежа

```python
point = (3, 5)        # через скобки
point = 3, 5          # без скобок
one = (42,)           # один элемент — запятая обязательна
empty = ()            # пустой
```

### 2.2. Распаковка

```python
point = (3, 5)
x, y = point
print(x, y)   # 3 5
```

### 2.3. Распаковка со `*`

```python
nums = (1, 2, 3, 4, 5)
first, *middle, last = nums
print(first)    # 1
print(middle)   # [2, 3, 4]
print(last)     # 5
```

### 2.4. Возврат нескольких значений

```python
def min_max(nums):
    return min(nums), max(nums)   # возвращает кортеж

lo, hi = min_max([3, 1, 7, 2])
print(lo, hi)   # 1 7
```

### 2.5. Обмен значений

```python
a, b = 5, 10
a, b = b, a   # обмен через кортеж
print(a, b)   # 10 5
```

---

## 3. Задания для выполнения

### 🟢 Уровень 1. Базовые задачи

#### Задание 1. Создание и вывод кортежа

**Условие:** создать кортеж из 5 чисел, вывести его и длину.

**Решение:**

```python
nums = (10, 20, 30, 40, 50)
print(nums)          # (10, 20, 30, 40, 50)
print(len(nums))     # 5
```

---

#### Задание 2. Доступ по индексу

**Условие:** вывести первый, последний и средний элементы кортежа.

**Решение:**

```python
nums = (10, 20, 30, 40, 50)
print(nums[0])     # 10
print(nums[-1])    # 50
print(nums[2])     # 30
```

---

#### Задание 3. Распаковка кортежа

**Условие:** распаковать кортеж `(1, 2, 3)` в переменные `a`, `b`, `c`.

**Решение:**

```python
a, b, c = (1, 2, 3)
print(a, b, c)   # 1 2 3
```

---

#### Задание 4. Обмен значений

**Условие:** обменять значения двух переменных через кортеж.

**Решение:**

```python
a = 5
b = 10

a, b = b, a
print(a, b)   # 10 5
```

---

#### Задание 5. Срез кортежа

**Условие:** вывести первый, последний и средние элементы через срез.

**Решение:**

```python
nums = (1, 2, 3, 4, 5)

print(nums[:2])    # (1, 2)
print(nums[-2:])   # (4, 5)
print(nums[1:4])   # (2, 3, 4)
print(nums[::-1])  # (5, 4, 3, 2, 1)
```

---

### 🟡 Уровень 2. Распаковка с `*`

#### Задание 6. Первый и остальные

**Условие:** распаковать кортеж на первый элемент и все остальные.

**Решение:**

```python
nums = (1, 2, 3, 4, 5)
first, *rest = nums

print(first)   # 1
print(rest)    # [2, 3, 4, 5]
```

---

#### Задание 7. Первый, последний и середина

**Условие:** распаковать кортеж на первый, последний и середину.

**Решение:**

```python
nums = (1, 2, 3, 4, 5)
first, *middle, last = nums

print(first)    # 1
print(middle)   # [2, 3, 4]
print(last)     # 5
```

---

#### Задание 8. Функция с возвратом кортежа

**Условие:** написать функцию, возвращающую сумму и произведение двух чисел.

**Решение:**

```python
def sum_prod(a, b):
    return a + b, a * b

s, p = sum_prod(3, 4)
print(f"Сумма: {s}, Произведение: {p}")
```

**Вывод:**
```
Сумма: 7, Произведение: 12
```

---

#### Задание 9. Min и max

**Условие:** написать функцию, возвращающую минимум и максимум списка.

**Решение:**

```python
def min_max(nums):
    return min(nums), max(nums)

lo, hi = min_max([5, 2, 8, 1, 9])
print(f"Min: {lo}, Max: {hi}")
```

**Вывод:**
```
Min: 1, Max: 9
```

---

#### Задание 10. Частное и остаток

**Условие:** написать функцию, возвращающую частное и остаток от деления.

**Решение:**

```python
def divmod_custom(a, b):
    return a // b, a % b

q, r = divmod_custom(17, 5)
print(f"17 = 5 × {q} + {r}")   # 17 = 5 × 3 + 2
```

---

### 🟠 Уровень 3. Кортежи в структурах

#### Задание 11. Список кортежей

**Условие:** дан список пар `(имя, возраст)`. Вывести каждого.

**Решение:**

```python
people = [("Иван", 25), ("Пётр", 20), ("Анна", 30)]

for name, age in people:
    print(f"{name} — {age} лет")
```

**Вывод:**
```
Иван — 25 лет
Пётр — 20 лет
Анна — 30 лет
```

---

#### Задание 12. Поиск по списку кортежей

**Условие:** найти человека по имени в списке пар.

**Решение:**

```python
people = [("Иван", 25), ("Пётр", 20), ("Анна", 30)]
target = "Пётр"

for name, age in people:
    if name == target:
        print(f"Найден: {name}, {age} лет")
        break
else:
    print("Не найден")
```

---

#### Задание 13. Координаты точек

**Условие:** дан список координат `(x, y)`. Найти точку, ближайшую к началу координат.

**Решение:**

```python
points = [(3, 4), (1, 1), (5, 12), (2, 2)]

nearest = None
min_dist = float("inf")

for x, y in points:
    dist = (x**2 + y**2) ** 0.5
    if dist < min_dist:
        min_dist = dist
        nearest = (x, y)

print(f"Ближайшая точка: {nearest}, расстояние: {min_dist:.2f}")
```

**Вывод:**
```
Ближайшая точка: (1, 1), расстояние: 1.41
```

---

#### Задание 14. Словарь с кортежами-ключами

**Условие:** создать словарь расстояний между городами через кортежи.

**Решение:**

```python
distances = {
    ("Москва", "Питер"): 700,
    ("Москва", "Казань"): 800,
    ("Питер", "Казань"): 1500
}

print(distances[("Москва", "Питер")])   # 700

# Обход
for (city1, city2), dist in distances.items():
    print(f"{city1} — {city2}: {dist} км")
```

---

#### Задание 15. Кортеж как возврат нескольких значений из функции

**Условие:** написать функцию, возвращающую статистику по списку: min, max, среднее.

**Решение:**

```python
def stats(nums):
    return min(nums), max(nums), sum(nums) / len(nums)

lo, hi, avg = stats([5, 2, 8, 1, 9])
print(f"Min: {lo}, Max: {hi}, Avg: {avg:.2f}")
```

**Вывод:**
```
Min: 1, Max: 9, Avg: 5.00
```

---

### 🔴 Уровень 4. Повышенной сложности

#### Задание 16. Функция с несколькими возвратами

**Условие:** написать функцию, которая по числам `a` и `b` возвращает сумму, разность, произведение и частное.

**Решение:**

```python
def all_operations(a, b):
    return a + b, a - b, a * b, a / b

s, d, p, q = all_operations(10, 3)
print(f"Сумма: {s}")
print(f"Разность: {d}")
print(f"Произведение: {p}")
print(f"Частное: {q:.2f}")
```

**Вывод:**
```
Сумма: 13
Разность: 7
Произведение: 30
Частное: 3.33
```

---

#### Задание 17. Сортировка списка кортежей

**Условие:** отсортировать список `(имя, балл)` по баллу.

**Решение:**

```python
students = [("Иван", 4.5), ("Пётр", 3.8), ("Анна", 4.9)]

students.sort(key=lambda s: s[1], reverse=True)

for name, grade in students:
    print(f"{name}: {grade}")
```

**Вывод:**
```
Анна: 4.9
Иван: 4.5
Пётр: 3.8
```

---

#### Задание 18. Группировка по ключу

**Условие:** дан список пар `(группа, студент)`. Сгруппировать студентов по группам.

**Решение:**

```python
data = [
    (214, "Иван"),
    (215, "Анна"),
    (214, "Пётр"),
    (215, "Мария")
]

groups = {}
for group, name in data:
    groups.setdefault(group, []).append(name)

for group, names in groups.items():
    print(f"Группа {group}: {', '.join(names)}")
```

**Вывод:**
```
Группа 214: Иван, Пётр
Группа 215: Анна, Мария
```

---

#### Задание 19. Возврат кортежа из рекурсивной функции

**Условие:** написать рекурсивную функцию, возвращающую `(min, max)` для списка.

**Решение:**

```python
def min_max_recursive(nums):
    if len(nums) == 1:
        return nums[0], nums[0]

    lo, hi = min_max_recursive(nums[1:])
    return min(nums[0], lo), max(nums[0], hi)

result = min_max_recursive([5, 2, 8, 1, 9])
print(f"Min: {result[0]}, Max: {result[1]}")
```

**Вывод:**
```
Min: 1, Max: 9
```

---

#### Задание 20. Множественная распаковка

**Условие:** распаковать вложенный кортеж.

**Решение:**

```python
data = (1, (2, 3), 4)

a, (b, c), d = data
print(a, b, c, d)   # 1 2 3 4
```

---

## 4. Самостоятельная работа

| Вариант | Задание |
|---------|---------|
| 1 | Создать кортеж, распаковать в переменные |
| 2 | Функция, возвращающая min и max из списка |
| 3 | Обмен значений трёх переменных через кортеж |
| 4 | Распаковка со `*` — первый, последний и середина |
| 5 | Список кортежей — вывести через распаковку |
| 6 | Функция, возвращающая частное и остаток |
| 7 | Найти точку с максимальным расстоянием от центра |
| 8 | Сортировка списка кортежей по второму элементу |
| 9 | Словарь с кортежами-ключами |
| 10 | Функция, возвращающая 4 арифметических результата |

---

## 5. Контрольные вопросы

1. Что такое кортеж?
2. Чем кортеж отличается от списка?
3. Как создать кортеж из одного элемента?
4. Что такое распаковка?
5. Что делает `*` при распаковке?
6. Как вернуть несколько значений из функции?
7. Как обменять значения через кортеж?
8. Можно ли использовать кортеж как ключ словаря?
9. Можно ли изменить кортеж?
10. Как обойти список кортежей?

---

## 6. Критерии оценки

| Оценка | Критерии |
|--------|----------|
| **«5»** | Все задания уровней 1–4 выполнены, код корректен |
| **«4»** | Выполнены задания уровней 1–3, есть незначительные ошибки |
| **«3»** | Выполнены задания уровней 1–2 |
| **«2»** | Большинство заданий не выполнено |

---

## 7. Компиляция и запуск

```bash
python task.py
```

**В VS Code:**
- Откройте файл.
- Нажмите `F5` или `Ctrl+F5`.

---

## 8. Домашнее задание

1. Дописать незаконченные задания.
2. Составить отчёт с примерами входных/выходных данных.
3. Ответить на контрольные вопросы.
4. Решить 2 задачи из самостоятельной работы.

---

## 9. Литература

1. Лутц М. «Программирование на Python». — Т. 1.
2. Мэтиз Э. «Изучаем Python». — Гл. 4.
3. Официальная документация Python: docs.python.org.

---

**Итог:** за 2 ч студенты решают **20 задач**, осваивая:
- создание и доступ к кортежам,
- распаковку (в том числе с `*`),
- возврат нескольких значений из функций,
- работу с кортежами в структурах,
- словари с кортежами-ключами.

